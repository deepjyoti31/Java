import java.io.*;
import java.math.*;
import java.security.*;
import java.text.*;
import java.util.*;
import java.util.concurrent.*;
import java.util.regex.*;

public class Solution {

        public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        
        double mealCost = sc.nextDouble();
        int tipPercent = sc.nextInt();
        int taxPercent = sc.nextInt();

        double total = (mealCost + (mealCost * taxPercent/100) + (mealCost * tipPercent/100));

       // System.out.println(total);

        int totalCost = (int) Math.round(total);

        System.out.println(totalCost);
        }
}


//You can find the question to the solution in the link below.
//https://www.hackerrank.com/challenges/30-operators/problem
